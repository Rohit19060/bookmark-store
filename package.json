{
  "name": "bookmark-store",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --open --port 3000 --mode development ",
    "build": "tsc && vite build",
    "preview": "vite preview --host --open"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "vite": "^4.1.1"
  }
}
